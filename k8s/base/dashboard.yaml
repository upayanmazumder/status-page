apiVersion: apps/v1
kind: Deployment
metadata:
  name: dashboard-service
  namespace: status-page
  labels:
    app: dashboard-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: dashboard-service
  template:
    metadata:
      labels:
        app: dashboard-service
        metrics: enabled
    spec:
      containers:
        - name: dashboard
          image: ghcr.io/YOUR_USERNAME/status-page/dashboard:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8000
          envFrom:
            - configMapRef:
                name: status-page-config
            - secretRef:
                name: status-page-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: dashboard-service
  namespace: status-page
spec:
  type: ClusterIP
  selector:
    app: dashboard-service
  ports:
    - port: 8000
      targetPort: 8000
      name: http
